generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cms_help {
  id          BigInt    @id @default(autoincrement())
  category_id BigInt?
  icon        String?   @db.VarChar(500)
  title       String?   @db.VarChar(100)
  show_status Int?
  create_time DateTime? @db.DateTime(0)
  read_count  Int?
  content     String?   @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cms_help_category {
  id          BigInt  @id @default(autoincrement())
  name        String? @db.VarChar(100)
  icon        String? @db.VarChar(500)
  help_count  Int?
  show_status Int?
  sort        Int?
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cms_member_report {
  id                 BigInt?
  report_type        Int?
  report_member_name String?   @db.VarChar(100)
  create_time        DateTime? @db.DateTime(0)
  report_object      String?   @db.VarChar(100)
  report_status      Int?
  handle_status      Int?
  note               String?   @db.VarChar(200)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cms_prefrence_area {
  id          BigInt  @id @default(autoincrement())
  name        String? @db.VarChar(255)
  sub_title   String? @db.VarChar(255)
  pic         Bytes?  @db.VarBinary(500)
  sort        Int?
  show_status Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cms_prefrence_area_product_relation {
  id                BigInt  @id @default(autoincrement())
  prefrence_area_id BigInt?
  product_id        BigInt?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cms_subject {
  id               BigInt    @id @default(autoincrement())
  category_id      BigInt?
  title            String?   @db.VarChar(100)
  pic              String?   @db.VarChar(500)
  product_count    Int?
  recommend_status Int?
  create_time      DateTime? @db.DateTime(0)
  collect_count    Int?
  read_count       Int?
  comment_count    Int?
  album_pics       String?   @db.VarChar(1000)
  description      String?   @db.VarChar(1000)
  show_status      Int?
  content          String?   @db.Text
  forward_count    Int?
  category_name    String?   @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cms_subject_category {
  id            BigInt  @id @default(autoincrement())
  name          String? @db.VarChar(100)
  icon          String? @db.VarChar(500)
  subject_count Int?
  show_status   Int?
  sort          Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cms_subject_comment {
  id               BigInt    @id @default(autoincrement())
  subject_id       BigInt?
  member_nick_name String?   @db.VarChar(255)
  member_icon      String?   @db.VarChar(255)
  content          String?   @db.VarChar(1000)
  create_time      DateTime? @db.DateTime(0)
  show_status      Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cms_subject_product_relation {
  id         BigInt  @id @default(autoincrement())
  subject_id BigInt?
  product_id BigInt?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cms_topic {
  id              BigInt    @id @default(autoincrement())
  category_id     BigInt?
  name            String?   @db.VarChar(255)
  create_time     DateTime? @db.DateTime(0)
  start_time      DateTime? @db.DateTime(0)
  end_time        DateTime? @db.DateTime(0)
  attend_count    Int?
  attention_count Int?
  read_count      Int?
  award_name      String?   @db.VarChar(100)
  attend_type     String?   @db.VarChar(100)
  content         String?   @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cms_topic_category {
  id            BigInt  @id @default(autoincrement())
  name          String? @db.VarChar(100)
  icon          String? @db.VarChar(500)
  subject_count Int?
  show_status   Int?
  sort          Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cms_topic_comment {
  id               BigInt    @id @default(autoincrement())
  member_nick_name String?   @db.VarChar(255)
  topic_id         BigInt?
  member_icon      String?   @db.VarChar(255)
  content          String?   @db.VarChar(1000)
  create_time      DateTime? @db.DateTime(0)
  show_status      Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oms_cart_item {
  id                  BigInt    @id @default(autoincrement())
  product_id          BigInt?
  product_sku_id      BigInt?
  member_id           BigInt?
  quantity            Int?
  price               Decimal?  @db.Decimal(10, 2)
  product_pic         String?   @db.VarChar(1000)
  product_name        String?   @db.VarChar(500)
  product_sub_title   String?   @db.VarChar(500)
  product_sku_code    String?   @db.VarChar(200)
  member_nickname     String?   @db.VarChar(500)
  create_date         DateTime? @db.DateTime(0)
  modify_date         DateTime? @db.DateTime(0)
  delete_status       Int?      @default(0)
  product_category_id BigInt?
  product_brand       String?   @db.VarChar(200)
  product_sn          String?   @db.VarChar(200)
  product_attr        String?   @db.VarChar(500)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oms_company_address {
  id             BigInt  @id @default(autoincrement())
  address_name   String? @db.VarChar(200)
  send_status    Int?
  receive_status Int?
  name           String? @db.VarChar(64)
  phone          String? @db.VarChar(64)
  province       String? @db.VarChar(64)
  city           String? @db.VarChar(64)
  region         String? @db.VarChar(64)
  detail_address String? @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oms_order {
  id                      BigInt    @id @default(autoincrement())
  member_id               BigInt
  coupon_id               BigInt?
  order_sn                String?   @db.VarChar(64)
  create_time             DateTime? @db.DateTime(0)
  member_username         String?   @db.VarChar(64)
  total_amount            Decimal?  @db.Decimal(10, 2)
  pay_amount              Decimal?  @db.Decimal(10, 2)
  freight_amount          Decimal?  @db.Decimal(10, 2)
  promotion_amount        Decimal?  @db.Decimal(10, 2)
  integration_amount      Decimal?  @db.Decimal(10, 2)
  coupon_amount           Decimal?  @db.Decimal(10, 2)
  discount_amount         Decimal?  @db.Decimal(10, 2)
  pay_type                Int?
  source_type             Int?
  status                  Int?
  order_type              Int?
  delivery_company        String?   @db.VarChar(64)
  delivery_sn             String?   @db.VarChar(64)
  auto_confirm_day        Int?
  integration             Int?
  growth                  Int?
  promotion_info          String?   @db.VarChar(100)
  bill_type               Int?
  bill_header             String?   @db.VarChar(200)
  bill_content            String?   @db.VarChar(200)
  bill_receiver_phone     String?   @db.VarChar(32)
  bill_receiver_email     String?   @db.VarChar(64)
  receiver_name           String    @db.VarChar(100)
  receiver_phone          String    @db.VarChar(32)
  receiver_post_code      String?   @db.VarChar(32)
  receiver_province       String?   @db.VarChar(32)
  receiver_city           String?   @db.VarChar(32)
  receiver_region         String?   @db.VarChar(32)
  receiver_detail_address String?   @db.VarChar(200)
  note                    String?   @db.VarChar(500)
  confirm_status          Int?
  delete_status           Int       @default(0)
  use_integration         Int?
  payment_time            DateTime? @db.DateTime(0)
  delivery_time           DateTime? @db.DateTime(0)
  receive_time            DateTime? @db.DateTime(0)
  comment_time            DateTime? @db.DateTime(0)
  modify_time             DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oms_order_item {
  id                  BigInt   @id @default(autoincrement())
  order_id            BigInt?
  order_sn            String?  @db.VarChar(64)
  product_id          BigInt?
  product_pic         String?  @db.VarChar(500)
  product_name        String?  @db.VarChar(200)
  product_brand       String?  @db.VarChar(200)
  product_sn          String?  @db.VarChar(64)
  product_price       Decimal? @db.Decimal(10, 2)
  product_quantity    Int?
  product_sku_id      BigInt?
  product_sku_code    String?  @db.VarChar(50)
  product_category_id BigInt?
  promotion_name      String?  @db.VarChar(200)
  promotion_amount    Decimal? @db.Decimal(10, 2)
  coupon_amount       Decimal? @db.Decimal(10, 2)
  integration_amount  Decimal? @db.Decimal(10, 2)
  real_amount         Decimal? @db.Decimal(10, 2)
  gift_integration    Int?     @default(0)
  gift_growth         Int?     @default(0)
  product_attr        String?  @db.VarChar(500)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oms_order_operate_history {
  id           BigInt    @id @default(autoincrement())
  order_id     BigInt?
  operate_man  String?   @db.VarChar(100)
  create_time  DateTime? @db.DateTime(0)
  order_status Int?
  note         String?   @db.VarChar(500)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oms_order_return_apply {
  id                 BigInt    @id @default(autoincrement())
  order_id           BigInt?
  company_address_id BigInt?
  product_id         BigInt?
  order_sn           String?   @db.VarChar(64)
  create_time        DateTime? @db.DateTime(0)
  member_username    String?   @db.VarChar(64)
  return_amount      Decimal?  @db.Decimal(10, 2)
  return_name        String?   @db.VarChar(100)
  return_phone       String?   @db.VarChar(100)
  status             Int?
  handle_time        DateTime? @db.DateTime(0)
  product_pic        String?   @db.VarChar(500)
  product_name       String?   @db.VarChar(200)
  product_brand      String?   @db.VarChar(200)
  product_attr       String?   @db.VarChar(500)
  product_count      Int?
  product_price      Decimal?  @db.Decimal(10, 2)
  product_real_price Decimal?  @db.Decimal(10, 2)
  reason             String?   @db.VarChar(200)
  description        String?   @db.VarChar(500)
  proof_pics         String?   @db.VarChar(1000)
  handle_note        String?   @db.VarChar(500)
  handle_man         String?   @db.VarChar(100)
  receive_man        String?   @db.VarChar(100)
  receive_time       DateTime? @db.DateTime(0)
  receive_note       String?   @db.VarChar(500)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oms_order_return_reason {
  id          BigInt    @id @default(autoincrement())
  name        String?   @db.VarChar(100)
  sort        Int?
  status      Int?
  create_time DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oms_order_setting {
  id                    BigInt @id @default(autoincrement())
  flash_order_overtime  Int?
  normal_order_overtime Int?
  confirm_overtime      Int?
  finish_overtime       Int?
  comment_overtime      Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_album {
  id          BigInt  @id @default(autoincrement())
  name        String? @db.VarChar(64)
  cover_pic   String? @db.VarChar(1000)
  pic_count   Int?
  sort        Int?
  description String? @db.VarChar(1000)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_album_pic {
  id       BigInt  @id @default(autoincrement())
  album_id BigInt?
  pic      String? @db.VarChar(1000)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_brand {
  id                    BigInt  @id @default(autoincrement())
  name                  String? @db.VarChar(64)
  first_letter          String? @db.VarChar(8)
  sort                  Int?
  factory_status        Int?
  show_status           Int?
  product_count         Int?
  product_comment_count Int?
  logo                  String? @db.VarChar(255)
  big_pic               String? @db.VarChar(255)
  brand_story           String? @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_comment {
  id                BigInt    @id @default(autoincrement())
  product_id        BigInt?
  member_nick_name  String?   @db.VarChar(255)
  product_name      String?   @db.VarChar(255)
  star              Int?
  member_ip         String?   @db.VarChar(64)
  create_time       DateTime? @db.DateTime(0)
  show_status       Int?
  product_attribute String?   @db.VarChar(255)
  collect_couont    Int?
  read_count        Int?
  content           String?   @db.Text
  pics              String?   @db.VarChar(1000)
  member_icon       String?   @db.VarChar(255)
  replay_count      Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_comment_replay {
  id               BigInt    @id @default(autoincrement())
  comment_id       BigInt?
  member_nick_name String?   @db.VarChar(255)
  member_icon      String?   @db.VarChar(255)
  content          String?   @db.VarChar(1000)
  create_time      DateTime? @db.DateTime(0)
  type             Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_feight_template {
  id              BigInt   @id @default(autoincrement())
  name            String?  @db.VarChar(64)
  charge_type     Int?
  first_weight    Decimal? @db.Decimal(10, 2)
  first_fee       Decimal? @db.Decimal(10, 2)
  continue_weight Decimal? @db.Decimal(10, 2)
  continme_fee    Decimal? @db.Decimal(10, 2)
  dest            String?  @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_member_price {
  id                BigInt   @id @default(autoincrement())
  product_id        BigInt?
  member_level_id   BigInt?
  member_price      Decimal? @db.Decimal(10, 2)
  member_level_name String?  @db.VarChar(100)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_product {
  id                            BigInt    @id @default(autoincrement())
  brand_id                      BigInt?
  product_category_id           BigInt?
  feight_template_id            BigInt?
  product_attribute_category_id BigInt?
  name                          String    @db.VarChar(200)
  pic                           String?   @db.VarChar(255)
  product_sn                    String    @db.VarChar(64)
  delete_status                 Int?
  publish_status                Int?
  new_status                    Int?
  recommand_status              Int?
  verify_status                 Int?
  sort                          Int?
  sale                          Int?
  price                         Decimal?  @db.Decimal(10, 2)
  promotion_price               Decimal?  @db.Decimal(10, 2)
  gift_growth                   Int?      @default(0)
  gift_point                    Int?      @default(0)
  use_point_limit               Int?
  sub_title                     String?   @db.VarChar(255)
  description                   String?   @db.Text
  original_price                Decimal?  @db.Decimal(10, 2)
  stock                         Int?
  low_stock                     Int?
  unit                          String?   @db.VarChar(16)
  weight                        Decimal?  @db.Decimal(10, 2)
  preview_status                Int?
  service_ids                   String?   @db.VarChar(64)
  keywords                      String?   @db.VarChar(255)
  note                          String?   @db.VarChar(255)
  album_pics                    String?   @db.VarChar(255)
  detail_title                  String?   @db.VarChar(255)
  detail_desc                   String?   @db.Text
  detail_html                   String?   @db.Text
  detail_mobile_html            String?   @db.Text
  promotion_start_time          DateTime? @db.DateTime(0)
  promotion_end_time            DateTime? @db.DateTime(0)
  promotion_per_limit           Int?
  promotion_type                Int?
  brand_name                    String?   @db.VarChar(255)
  product_category_name         String?   @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_product_attribute {
  id                            BigInt  @id @default(autoincrement())
  product_attribute_category_id BigInt?
  name                          String? @db.VarChar(64)
  select_type                   Int?
  input_type                    Int?
  input_list                    String? @db.VarChar(255)
  sort                          Int?
  filter_type                   Int?
  search_type                   Int?
  related_status                Int?
  hand_add_status               Int?
  type                          Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_product_attribute_category {
  id              BigInt  @id @default(autoincrement())
  name            String? @db.VarChar(64)
  attribute_count Int?    @default(0)
  param_count     Int?    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_product_attribute_value {
  id                   BigInt  @id @default(autoincrement())
  product_id           BigInt?
  product_attribute_id BigInt?
  value                String? @db.VarChar(64)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_product_category {
  id            BigInt  @id @default(autoincrement())
  parent_id     BigInt?
  name          String? @db.VarChar(64)
  level         Int?
  product_count Int?
  product_unit  String? @db.VarChar(64)
  nav_status    Int?
  show_status   Int?
  sort          Int?
  icon          String? @db.VarChar(255)
  keywords      String? @db.VarChar(255)
  description   String? @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_product_category_attribute_relation {
  id                   BigInt  @id @default(autoincrement())
  product_category_id  BigInt?
  product_attribute_id BigInt?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_product_full_reduction {
  id           BigInt   @id @default(autoincrement())
  product_id   BigInt?
  full_price   Decimal? @db.Decimal(10, 2)
  reduce_price Decimal? @db.Decimal(10, 2)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_product_ladder {
  id         BigInt   @id @default(autoincrement())
  product_id BigInt?
  count      Int?
  discount   Decimal? @db.Decimal(10, 2)
  price      Decimal? @db.Decimal(10, 2)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_product_operate_log {
  id                  BigInt    @id @default(autoincrement())
  product_id          BigInt?
  price_old           Decimal?  @db.Decimal(10, 2)
  price_new           Decimal?  @db.Decimal(10, 2)
  sale_price_old      Decimal?  @db.Decimal(10, 2)
  sale_price_new      Decimal?  @db.Decimal(10, 2)
  gift_point_old      Int?
  gift_point_new      Int?
  use_point_limit_old Int?
  use_point_limit_new Int?
  operate_man         String?   @db.VarChar(64)
  create_time         DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_product_vertify_record {
  id          BigInt    @id @default(autoincrement())
  product_id  BigInt?
  create_time DateTime? @db.DateTime(0)
  vertify_man String?   @db.VarChar(64)
  status      Int?
  detail      String?   @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pms_sku_stock {
  id              BigInt   @id @default(autoincrement())
  product_id      BigInt?
  sku_code        String   @db.VarChar(64)
  price           Decimal? @db.Decimal(10, 2)
  stock           Int?     @default(0)
  low_stock       Int?
  pic             String?  @db.VarChar(255)
  sale            Int?
  promotion_price Decimal? @db.Decimal(10, 2)
  lock_stock      Int?     @default(0)
  sp_data         String?  @db.VarChar(500)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_coupon {
  id            BigInt    @id @default(autoincrement())
  type          Int?
  name          String?   @db.VarChar(100)
  platform      Int?
  count         Int?
  amount        Decimal?  @db.Decimal(10, 2)
  per_limit     Int?
  min_point     Decimal?  @db.Decimal(10, 2)
  start_time    DateTime? @db.DateTime(0)
  end_time      DateTime? @db.DateTime(0)
  use_type      Int?
  note          String?   @db.VarChar(200)
  publish_count Int?
  use_count     Int?
  receive_count Int?
  enable_time   DateTime? @db.DateTime(0)
  code          String?   @db.VarChar(64)
  member_level  Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_coupon_history {
  id              BigInt    @id @default(autoincrement())
  coupon_id       BigInt?
  member_id       BigInt?
  coupon_code     String?   @db.VarChar(64)
  member_nickname String?   @db.VarChar(64)
  get_type        Int?
  create_time     DateTime? @db.DateTime(0)
  use_status      Int?
  use_time        DateTime? @db.DateTime(0)
  order_id        BigInt?
  order_sn        String?   @db.VarChar(100)

  @@index([coupon_id], map: "idx_coupon_id")
  @@index([member_id], map: "idx_member_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_coupon_product_category_relation {
  id                    BigInt  @id @default(autoincrement())
  coupon_id             BigInt?
  product_category_id   BigInt?
  product_category_name String? @db.VarChar(200)
  parent_category_name  String? @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_coupon_product_relation {
  id           BigInt  @id @default(autoincrement())
  coupon_id    BigInt?
  product_id   BigInt?
  product_name String? @db.VarChar(500)
  product_sn   String? @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_flash_promotion {
  id          BigInt    @id @default(autoincrement())
  title       String?   @db.VarChar(200)
  start_date  DateTime? @db.Date
  end_date    DateTime? @db.Date
  status      Int?
  create_time DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_flash_promotion_log {
  id             Int       @id @default(autoincrement())
  member_id      Int?
  product_id     BigInt?
  member_phone   String?   @db.VarChar(64)
  product_name   String?   @db.VarChar(100)
  subscribe_time DateTime? @db.DateTime(0)
  send_time      DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_flash_promotion_product_relation {
  id                         BigInt   @id @default(autoincrement())
  flash_promotion_id         BigInt?
  flash_promotion_session_id BigInt?
  product_id                 BigInt?
  flash_promotion_price      Decimal? @db.Decimal(10, 2)
  flash_promotion_count      Int?
  flash_promotion_limit      Int?
  sort                       Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_flash_promotion_session {
  id          BigInt    @id @default(autoincrement())
  name        String?   @db.VarChar(200)
  start_time  DateTime? @db.Time(0)
  end_time    DateTime? @db.Time(0)
  status      Int?
  create_time DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_home_advertise {
  id          BigInt    @id @default(autoincrement())
  name        String?   @db.VarChar(100)
  type        Int?
  pic         String?   @db.VarChar(500)
  start_time  DateTime? @db.DateTime(0)
  end_time    DateTime? @db.DateTime(0)
  status      Int?
  click_count Int?
  order_count Int?
  url         String?   @db.VarChar(500)
  note        String?   @db.VarChar(500)
  sort        Int?      @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_home_brand {
  id               BigInt  @id @default(autoincrement())
  brand_id         BigInt?
  brand_name       String? @db.VarChar(64)
  recommend_status Int?
  sort             Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_home_new_product {
  id               BigInt  @id @default(autoincrement())
  product_id       BigInt?
  product_name     String? @db.VarChar(500)
  recommend_status Int?
  sort             Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_home_recommend_product {
  id               BigInt  @id @default(autoincrement())
  product_id       BigInt?
  product_name     String? @db.VarChar(500)
  recommend_status Int?
  sort             Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sms_home_recommend_subject {
  id               BigInt  @id @default(autoincrement())
  subject_id       BigInt?
  subject_name     String? @db.VarChar(64)
  recommend_status Int?
  sort             Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_admin {
  id          BigInt    @id @default(autoincrement())
  username    String?   @db.VarChar(64)
  password    String?   @db.VarChar(64)
  icon        String?   @db.VarChar(500)
  email       String?   @db.VarChar(100)
  nick_name   String?   @db.VarChar(200)
  note        String?   @db.VarChar(500)
  create_time DateTime? @db.DateTime(0)
  login_time  DateTime? @db.DateTime(0)
  status      Int?      @default(1)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_admin_login_log {
  id          BigInt    @id @default(autoincrement())
  admin_id    BigInt?
  create_time DateTime? @db.DateTime(0)
  ip          String?   @db.VarChar(64)
  address     String?   @db.VarChar(100)
  user_agent  String?   @db.VarChar(100)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_admin_permission_relation {
  id            BigInt  @id @default(autoincrement())
  admin_id      BigInt?
  permission_id BigInt?
  type          Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_admin_role_relation {
  id       BigInt  @id @default(autoincrement())
  admin_id BigInt?
  role_id  BigInt?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_growth_change_history {
  id           BigInt    @id @default(autoincrement())
  member_id    BigInt?
  create_time  DateTime? @db.DateTime(0)
  change_type  Int?
  change_count Int?
  operate_man  String?   @db.VarChar(100)
  operate_note String?   @db.VarChar(200)
  source_type  Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_integration_change_history {
  id           BigInt    @id @default(autoincrement())
  member_id    BigInt?
  create_time  DateTime? @db.DateTime(0)
  change_type  Int?
  change_count Int?
  operate_man  String?   @db.VarChar(100)
  operate_note String?   @db.VarChar(200)
  source_type  Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_integration_consume_setting {
  id                    BigInt @id @default(autoincrement())
  deduction_per_amount  Int?
  max_percent_per_order Int?
  use_unit              Int?
  coupon_status         Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_member {
  id                     BigInt    @id @default(autoincrement())
  member_level_id        BigInt?
  username               String?   @unique(map: "idx_username") @db.VarChar(64)
  password               String?   @db.VarChar(64)
  nickname               String?   @db.VarChar(64)
  phone                  String?   @unique(map: "idx_phone") @db.VarChar(64)
  status                 Int?
  create_time            DateTime? @db.DateTime(0)
  icon                   String?   @db.VarChar(500)
  gender                 Int?
  birthday               DateTime? @db.Date
  city                   String?   @db.VarChar(64)
  job                    String?   @db.VarChar(100)
  personalized_signature String?   @db.VarChar(200)
  source_type            Int?
  integration            Int?
  growth                 Int?
  luckey_count           Int?
  history_integration    Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_member_level {
  id                      BigInt   @id @default(autoincrement())
  name                    String?  @db.VarChar(100)
  growth_point            Int?
  default_status          Int?
  free_freight_point      Decimal? @db.Decimal(10, 2)
  comment_growth_point    Int?
  priviledge_free_freight Int?
  priviledge_sign_in      Int?
  priviledge_comment      Int?
  priviledge_promotion    Int?
  priviledge_member_price Int?
  priviledge_birthday     Int?
  note                    String?  @db.VarChar(200)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_member_login_log {
  id          BigInt    @id @default(autoincrement())
  member_id   BigInt?
  create_time DateTime? @db.DateTime(0)
  ip          String?   @db.VarChar(64)
  city        String?   @db.VarChar(64)
  login_type  Int?
  province    String?   @db.VarChar(64)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_member_member_tag_relation {
  id        BigInt  @id @default(autoincrement())
  member_id BigInt?
  tag_id    BigInt?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_member_product_category_relation {
  id                  BigInt  @id @default(autoincrement())
  member_id           BigInt?
  product_category_id BigInt?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_member_receive_address {
  id             BigInt  @id @default(autoincrement())
  member_id      BigInt?
  name           String? @db.VarChar(100)
  phone_number   String? @db.VarChar(64)
  default_status Int?
  post_code      String? @db.VarChar(100)
  province       String? @db.VarChar(100)
  city           String? @db.VarChar(100)
  region         String? @db.VarChar(100)
  detail_address String? @db.VarChar(128)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_member_rule_setting {
  id                  BigInt   @id @default(autoincrement())
  continue_sign_day   Int?
  continue_sign_point Int?
  consume_per_point   Decimal? @db.Decimal(10, 2)
  low_order_amount    Decimal? @db.Decimal(10, 2)
  max_point_per_order Int?
  type                Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_member_statistics_info {
  id                    BigInt    @id @default(autoincrement())
  member_id             BigInt?
  consume_amount        Decimal?  @db.Decimal(10, 2)
  order_count           Int?
  coupon_count          Int?
  comment_count         Int?
  return_order_count    Int?
  login_count           Int?
  attend_count          Int?
  fans_count            Int?
  collect_product_count Int?
  collect_subject_count Int?
  collect_topic_count   Int?
  collect_comment_count Int?
  invite_friend_count   Int?
  recent_order_time     DateTime? @db.DateTime(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_member_tag {
  id                  BigInt   @id @default(autoincrement())
  name                String?  @db.VarChar(100)
  finish_order_count  Int?
  finish_order_amount Decimal? @db.Decimal(10, 2)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_member_task {
  id           BigInt  @id @default(autoincrement())
  name         String? @db.VarChar(100)
  growth       Int?
  intergration Int?
  type         Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_menu {
  id          BigInt    @id @default(autoincrement())
  parent_id   BigInt?
  create_time DateTime? @db.DateTime(0)
  title       String?   @db.VarChar(100)
  level       Int?
  sort        Int?
  name        String?   @db.VarChar(100)
  icon        String?   @db.VarChar(200)
  hidden      Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_permission {
  id          BigInt    @id @default(autoincrement())
  pid         BigInt?
  name        String?   @db.VarChar(100)
  value       String?   @db.VarChar(200)
  icon        String?   @db.VarChar(500)
  type        Int?
  uri         String?   @db.VarChar(200)
  status      Int?
  create_time DateTime? @db.DateTime(0)
  sort        Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_resource {
  id          BigInt    @id @default(autoincrement())
  create_time DateTime? @db.DateTime(0)
  name        String?   @db.VarChar(200)
  url         String?   @db.VarChar(200)
  description String?   @db.VarChar(500)
  category_id BigInt?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_resource_category {
  id          BigInt    @id @default(autoincrement())
  create_time DateTime? @db.DateTime(0)
  name        String?   @db.VarChar(200)
  sort        Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_role {
  id          BigInt    @id @default(autoincrement())
  name        String?   @db.VarChar(100)
  description String?   @db.VarChar(500)
  admin_count Int?
  create_time DateTime? @db.DateTime(0)
  status      Int?      @default(1)
  sort        Int?      @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_role_menu_relation {
  id      BigInt  @id @default(autoincrement())
  role_id BigInt?
  menu_id BigInt?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_role_permission_relation {
  id            BigInt  @id @default(autoincrement())
  role_id       BigInt?
  permission_id BigInt?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ums_role_resource_relation {
  id          BigInt  @id @default(autoincrement())
  role_id     BigInt?
  resource_id BigInt?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model x_cms_help {
  id          BigInt    @id @default(autoincrement())
  category_id BigInt?
  icon        String?   @db.VarChar(500)
  title       String?   @db.VarChar(100)
  show_status Int?
  create_time DateTime? @db.DateTime(0)
  read_count  Int?
  content     String?   @db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model x_cms_help_category {
  id          BigInt  @id @default(autoincrement())
  name        String? @db.VarChar(100)
  icon        String? @db.VarChar(500)
  help_count  Int?
  show_status Int?
  sort        Int?
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model x_cms_member_report {
  id                 BigInt?
  report_type        Int?
  report_member_name String?   @db.VarChar(100)
  create_time        DateTime? @db.DateTime(0)
  report_object      String?   @db.VarChar(100)
  report_status      Int?
  handle_status      Int?
  note               String?   @db.VarChar(200)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model x_cms_prefrence_area {
  id          BigInt  @id @default(autoincrement())
  name        String? @db.VarChar(255)
  sub_title   String? @db.VarChar(255)
  pic         Bytes?  @db.VarBinary(500)
  sort        Int?
  show_status Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model x_pms_product_ladder {
  id         BigInt   @id @default(autoincrement())
  product_id BigInt?
  count      Int?
  discount   Decimal? @db.Decimal(10, 2)
  price      Decimal? @db.Decimal(10, 2)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model x_ums_admin_role_relation {
  id       BigInt  @id @default(autoincrement())
  admin_id BigInt?
  role_id  BigInt?
}
